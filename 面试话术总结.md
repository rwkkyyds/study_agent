# RAG 系统工程师 — 面试话术总结

> 基于 Week 1（Naive RAG 全链路）+ Week 2（Advanced RAG，Section 1-3）整理

---

## 一、自我介绍话术

> 我系统学习了 RAG（检索增强生成）的完整技术栈，从工程落地到生产优化。能独立搭建从文档加载、分块、向量化、检索到 LLM 生成的端到端 RAG 系统，并用 FastAPI 封装成 API 服务、Docker 容器化部署。在优化层面，掌握了 Query Transformation（HyDE、多查询改写）、混合检索（BM25 + 向量检索 + RRF 融合）、Rerank 重排序，以及 RAGAs/DeepEval 自动化评估体系。

---

## 二、核心技术栈 & 话术

### 1. RAG 全链路（必问）

**话术：**
> RAG 的核心思路是"先检索、再生成"。我把整个流程分为索引阶段和查询阶段。
> 索引阶段：原始文档通过 Document Loader 加载为标准 Document 对象，然后用 RecursiveCharacterTextSplitter 按语义边界递归分块（chunk_size 200-500，overlap 10%-20%），再通过 Embedding 模型转为向量存入向量数据库。
> 查询阶段：用户问题先 Embedding，然后在向量库中做相似度检索拿到 Top-K 文档，拼接进 Prompt 作为 context，交给 LLM 生成最终回答。
> 关键设计：Prompt 里会写"只根据参考资料回答，没有就直说"来抑制幻觉，temperature 设 0.3 保证输出稳定。

**追问：chunk_size 怎么选？**
> chunk_size 太小会导致语义碎片化，检索到的内容不完整；太大会引入噪音、浪费 token。经验值 200-1000 字符，需要根据实际业务场景调优。chunk_overlap 一般设 chunk_size 的 10%-20%，防止句子在块边界被切断。

---

### 2. LCEL（LangChain Expression Language）

**话术：**
> LangChain 的 LCEL 用管道符 `|` 把组件串联，每个组件都实现统一的 Runnable 接口（invoke/batch/stream）。比如 RAG 链可以写成 `prompt | llm | parser`，数据从左往右流动。好处是组件可以自由替换——换 LLM、换 Parser 不影响其他部分，而且原生支持流式输出。

**追问：LCEL 和直接调用有什么区别？**
> 本质一样，但 LCEL 提供了声明式的组合方式，链可以像乐高一样复用。比如翻译链和摘要链可以组合成"先翻译再摘要"的复合链。后续 RAG 的检索链、Agent 的工具调用链都基于 LCEL。

---

### 3. Query Transformation（HyDE + 多查询改写）

**话术：**
> 用户原始问题直接做向量检索效果往往不好，因为问题太短、口语化，和文档的书面表述有语义鸿沟。我在检索前加了一层 Query Transformation。
> HyDE 的思路是用 LLM 先生成一段假设性答案文档，再用这个假设文档去做 Embedding 和检索。相当于用"答案"找"答案"，语义匹配更准。
> 多查询改写是把一个问题改写成多个不同角度的子问题，分别检索后合并去重，显著扩大召回范围。
> 两者可以组合使用，HyDE 适合单轮精确检索，多查询改写适合需要高召回的场景。

**追问：HyDE 的代价是什么？**
> 多一次 LLM 调用，增加延迟和成本。而且假设文档的质量取决于 LLM 能力，如果 LLM 生成的内容有偏差，反而会引入噪音。

---

### 4. 混合检索 + RRF 融合

**话术：**
> 单一向量检索有局限——语义检索擅长理解含义但精确匹配弱，关键词检索（BM25）擅长精确匹配但无法理解语义。所以我用混合检索把两者结合。
> 具体做法是 BM25 和向量检索各召回 Top-N，然后用 RRF（Reciprocal Rank Fusion）做融合排序。RRF 的公式是 `score = Σ 1/(k + rank_i)`，k 通常取 60。RRF 只看排名不看分数，所以不同检索器的分数尺度不同也没关系，实践中效果很稳定。

**追问：为什么用 RRF 而不是加权求和？**
> 因为 BM25 的分数和向量相似度的分数尺度完全不同，没法直接加权。RRF 只看排名，天然解决了这个问题。

---

### 5. Rerank 重排序（两阶段检索）

**话术：**
> 生产级 RAG 一般用两阶段检索架构。第一阶段召回：BM25 + 向量检索 → 粗筛 Top-20；第二阶段精排：用 Cross-Encoder 模型（如 bge-reranker）对 query 和每个文档拼接后一起编码，逐个打分，重排序输出 Top-5。
> Cross-Encoder 比 Bi-Encoder 精度高，因为它是把问题和文档拼接后一起过模型，能捕捉更细粒度的交互特征。代价是速度慢，所以只在精排阶段用。

**追问：Bi-Encoder 和 Cross-Encoder 的区别？**
> Bi-Encoder 是把问题和文档分别编码成向量，然后算相似度，速度快，适合召回阶段；Cross-Encoder 是把问题和文档拼接后一起编码，精度高但速度慢，适合精排阶段。召回用 Bi-Encoder 粗筛，精排用 Cross-Encoder 细选。

---

### 6. RAG 评估体系（RAGAs + DeepEval）

**话术：**
> RAG 系统搭好后，我用 RAGAs 和 DeepEval 两个框架做自动化评估。核心评估四个维度：
> - Faithfulness（忠实度）：回答是否基于检索文档，有没有编造
> - Answer Relevancy（答案相关性）：回答是否切题
> - Context Precision（上下文精确度）：相关文档排名是否靠前
> - Context Recall（上下文召回率）：相关文档是否都被检索到
> 开发阶段用 DeepEval 集成 pytest，每次提交自动评估；定期用 RAGAs 生成批量评估报告做趋势分析。

**追问：Faithfulness 低怎么排查？**
> Faithfulness < 0.8 说明 LLM 在"编造"，排查方向：1）Prompt 有没有写"只根据参考资料回答"；2）temperature 是不是太高；3）检索到的文档是否真的包含答案。

---

### 7. FastAPI 工程化

**话术：**
> RAG 系统对外暴露 API 我用 FastAPI，三大优势：性能接近 Go（基于 Starlette + Pydantic）、自动生成 Swagger 文档、类型安全。
> 请求体用 Pydantic BaseModel 做数据校验，不符合规则自动返回 422 错误。涉及 IO 操作（如调用 LLM、查向量库）用 async/await 异步处理，避免阻塞。

---

### 8. Docker 容器化部署

**话术：**
> RAG API 用 Docker 打包部署。Dockerfile 里先 COPY requirements.txt 再 COPY 代码，利用 Docker 缓存层——改代码不用重装依赖。用 uvicorn 作为 ASGI 服务器启动，映射端口对外服务。

---

## 三、高频面试问题 & 标准答案

### Q1: RAG 和微调怎么选？
> RAG 适合知识频繁更新、需要引用来源的场景，成本低、可解释性强。微调适合需要改变模型风格/能力的场景。生产中两者可以结合——微调让模型更擅长利用检索结果，RAG 提供最新知识。

### Q2: 向量检索的 ANN 算法了解哪些？
> FAISS 的 Flat（暴力搜索，精确但慢）、IVF（倒排索引，分桶加速）、HNSW（图搜索，速度和精度平衡）。生产环境 Milvus 默认用 IVF_FLAT 或 HNSW。

### Q3: Embedding 模型怎么选？
> 商用选 OpenAI text-embedding-3-small（1536维），中文开源选 BAAI/bge-small-zh-v1.5（512维，免费、本地部署）。选型考虑：维度大小影响存储和检索速度，中文场景优先选中文预训练模型。

### Q4: RAG 系统上线后怎么监控？
> 1）日志记录每次检索的 Top-K 文档和 LLM 回答；2）定期用 RAGAs 跑评估指标；3）收集用户反馈（点赞/点踩）；4）监控延迟和错误率。

### Q5: 幻觉怎么解决？
> 1）Prompt 层：写"只根据参考资料回答，没有就直说"；2）检索层：提高召回率和精确度，确保相关文档被检索到；3）评估层：用 Faithfulness 指标监控，低于阈值报警；4）temperature 设低（0.3）减少随机性。

---

## 四、项目经验话术（STAR 法则）

### 项目：知识库问答系统

**Situation（背景）：**
> 企业内部有大量技术文档，员工查资料效率低，想做一个智能问答系统。

**Task（任务）：**
> 搭建一个端到端的 RAG 系统，支持文档上传、自动分块、语义检索、LLM 生成回答，并对外提供 API 服务。

**Action（行动）：**
> 1）用 LangChain Document Loader 加载多格式文档，RecursiveCharacterTextSplitter 按语义边界分块
> 2）用 BGE 模型做 Embedding，FAISS 做向量存储和检索
> 3）实现混合检索（BM25 + 向量检索 + RRF 融合）+ Rerank 重排序，提高检索质量
> 4）用 LCEL 构建 RAG 链，FastAPI 封装 API，Docker 容器化部署
> 5）用 RAGAs + DeepEval 建立自动化评估体系，Faithfulness > 0.85

**Result（结果）：**
> 系统上线后员工查文档效率提升 60%，回答准确率（Faithfulness）稳定在 0.85+，API 平均响应时间 < 2s。

---

## 五、技术关键词速查表

| 关键词 | 一句话解释 |
|--------|-----------|
| RAG | 检索增强生成，先检索外部知识再让 LLM 生成回答 |
| Embedding | 把文本转为高维向量，语义相似的文本向量距离近 |
| 余弦相似度 | 衡量两个向量的方向相似程度，值域 [-1, 1] |
| RecursiveCharacterTextSplitter | 按语义边界递归分块，最推荐的分块器 |
| chunk_overlap | 相邻块重叠部分，防止句子在边界被切断 |
| LCEL | LangChain 管道符语法，用 `|` 串联组件 |
| HyDE | 用 LLM 生成假设性文档再检索，解决语义鸿沟 |
| BM25 | 基于词频和逆文档频率的关键词检索算法 |
| RRF | 基于排名的多路检索融合策略，不看分数只看排名 |
| Cross-Encoder | 把 query 和 doc 拼接后一起编码，精度高适合精排 |
| Bi-Encoder | query 和 doc 分别编码，速度快适合召回 |
| Faithfulness | RAG 评估指标，衡量回答是否忠实于检索文档 |
| RAGAs | RAG 自动化评估框架 |
| DeepEval | 支持 pytest 集成的 RAG 评估框架 |
| FastAPI | 高性能 Python Web 框架，自动生成 API 文档 |
| Pydantic | 数据校验库，FastAPI 的核心依赖 |
| Docker | 容器化部署工具，保证环境一致性 |
