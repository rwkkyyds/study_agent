# 项目目录结构（阶段一）

```
项目一_企业级智能客服RAG系统/
│
├── app/                                    # 应用核心代码
│   ├── __init__.py                         # 应用包标记
│   ├── main.py                             # FastAPI 入口，lifespan 管理，路由注册
│   │
│   ├── core/                               # 核心基础设施
│   │   ├── __init__.py
│   │   └── config.py                       # Settings 类：集中管理环境变量
│   │
│   ├── db/                                 # 数据库层
│   │   ├── __init__.py
│   │   └── session.py                      # SQLAlchemy 引擎、会话工厂、Base 基类
│   │
│   ├── models/                             # 数据模型（ORM）
│   │   ├── __init__.py
│   │   ├── user.py                         # User：用户、角色、密码哈希
│   │   ├── document.py                     # Document + Chunk：文档与切分块
│   │   └── ticket.py                       # Ticket + Message：工单与消息
│   │
│   ├── schemas/                            # 请求/响应模型（Pydantic）
│   │   ├── __init__.py
│   │   └── auth.py                         # 登录、注册、用户信息 Schema
│   │
│   ├── services/                           # 业务逻辑服务
│   │   ├── __init__.py
│   │   └── auth.py                         # 认证服务：注册、登录、JWT、角色校验
│   │
│   └── api/                                # API 路由层
│       ├── __init__.py
│       └── auth.py                         # /auth/register, /auth/login, /auth/me
│
├── tests/                                   # 测试套件
│   ├── test_health.py                       # 健康检查接口测试（阶段一）
│   └── test_auth.py                         # 认证接口测试（阶段二）
│
├── docs/                                    # 项目文档
│   ├── README.md                            # 文档索引
│   ├── 01-项目总览/                          # 项目定位、技术栈、架构
│   └── 02-阶段一_项目骨架/                    # 阶段一文档
│
├── requirements.txt                         # Python 依赖清单
├── .env.example                             # 环境变量配置模板
└── README.md                                # 项目 README
```

## 文件分类说明

### 基础设施层（core/ + db/）
| 文件 | 核心类/函数 | 用途 |
|------|------------|------|
| `config.py` | `class Settings` + `get_settings()` | 管理所有环境变量，缓存单例 |
| `session.py` | `class Base` + `init_db()` + `get_db()` | 数据库引擎、建表、会话注入 |

### 数据模型层（models/）
| 文件 | 核心类 | 用途 |
|------|--------|------|
| `user.py` | `class User` | 用户登录、角色权限、密码哈希 |
| `document.py` | `class Document`, `class Chunk` | 文档元数据、切分块 |
| `ticket.py` | `class Ticket`, `class Message` | 工单状态流转、消息记录 |

### 业务逻辑层（services/）
| 文件 | 核心函数 | 用途 |
|------|----------|------|
| `auth.py` | `create_user()`, `authenticate_user()`, `create_access_token()`, `get_current_user()`, `require_role()` | 注册、登录、JWT 生成、身份校验、角色权限 |

### API 路由层（api/）
| 文件 | 路由 | 用途 |
|------|------|------|
| `auth.py` | `POST /auth/register` | 注册新用户 |
| | `POST /auth/login` | 登录获取 JWT |
| | `GET /auth/me` | 获取当前用户信息（需登录） |

### 测试层（tests/）
| 文件 | 测试范围 | 数量 |
|------|----------|------|
| `test_health.py` | 健康检查、版本号 | 2 个 |
| `test_auth.py` | 注册、登录、Token、角色 | 8 个 |

## 后续阶段将新增

### 阶段三：知识库入库与检索
- `app/rag/embeddings.py` — MockEmbedding
- `app/rag/vector_store.py` — InMemoryVectorStore
- `app/rag/chunker.py` — TextChunker
- `app/rag/retriever.py` — Retriever
- `tests/test_rag.py` — RAG 测试